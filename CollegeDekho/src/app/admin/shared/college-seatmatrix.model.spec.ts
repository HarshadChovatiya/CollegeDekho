import { CollegeSeatmatrix } from './college-seatmatrix.model';

describe('CollegeSeatmatrix', () => {
  it('should create an instance', () => {
    expect(new CollegeSeatmatrix()).toBeTruthy();
  });
});
