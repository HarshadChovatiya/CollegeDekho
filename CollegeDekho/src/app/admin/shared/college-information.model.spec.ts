import { CollegeInformation } from './college-information.model';

describe('CollegeInformation', () => {
  it('should create an instance', () => {
    expect(new CollegeInformation()).toBeTruthy();
  });
});
