import { CollegePlacement } from './college-placement.model';

describe('CollegePlacement', () => {
  it('should create an instance', () => {
    expect(new CollegePlacement()).toBeTruthy();
  });
});
